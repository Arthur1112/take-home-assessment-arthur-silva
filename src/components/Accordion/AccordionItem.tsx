interface AccordionItemProps {
  index: number;
  content: string;
  trigger: string;
  isOpen: boolean;
}

export const AccordionItem = ({
  index,
  content,
  trigger,
  isOpen,
}: AccordionItemProps) => {
  return <h1>Hey not this is accordionItem</h1>;
};
